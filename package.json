{
  "name": "project-lerna",
  "private": true,
  "devDependencies": {
    "lerna": "4"
  },
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "release:pre": "yarn lerna publish --conventional-prerelease  --yes",
    "release": "yarn lerna publish --conventional-commits --yes",
    "test": "yarn lerna run test --since origin/master",
    "generate": "lerna create"
  }
}
