{
  "name": "project-lerna",
  "private": false,
  "devDependencies": {
    "lerna": "^3.21.0"
  },
  "scripts": {
    "ship": "yarn lerna publish",
    "release": "yarn registry && yarn ship" ,
    "registry": "npm config set registry https://npm.pkg.github.com",
    "create": "lerna create"
  }
}
