{
  "name": "project-lerna",
  "private": false,
  "devDependencies": {
    "lerna": "^3.21.0"
  },
  "scripts": {
    "release": "yarn lerna publish --conventional-commits --create-release github --yes",
    "generate": "lerna create"
  }
}
